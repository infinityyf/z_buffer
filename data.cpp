#include <vector>
#include "vectors.h"
#include "trangle.h"
#include <string>

using namespace::std;

vector<string>  point_xyz(10);
vector<point>   points(10);
vector<string>  indexs(10);
vector<trangle> trangles(10);
vector<string>  colors(10);